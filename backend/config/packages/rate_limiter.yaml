framework:
    rate_limiter:
        register:
            policy: 'sliding_window'
            limit: 3
            interval: '1 hour'

when@test:
    framework:
        cache:
            pools:
                cache.rate_limiter:
                    adapter: cache.adapter.filesystem
